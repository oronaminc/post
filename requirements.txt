# --- Core (필수) ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
feedparser>=6.0
beautifulsoup4>=4.12
PyYAML>=6.0
APScheduler>=3.10
python-dotenv>=1.0

# --- Optional (pytrends 인기도 어댑터용) ---
# pandas를 함께 설치하므로 무겁습니다. 설치가 실패하거나 필요 없으면
# 아래 줄을 지워도 됩니다. 어댑터는 import 실패 시 자동 비활성화됩니다.
pytrends>=4.9
